import makeStyles from '@mui/styles/makeStyles';

const useStyles = makeStyles(() => ({
  root: {
    margin: '2rem auto 0',
  },
  paper: {
    padding: '2rem 3rem',
    minWidth: '100%',
  },
}));

export default useStyles;
